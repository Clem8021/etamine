<%= render "shared/navbar" %>

<div class="admin-dashboard">
  <h1 class="dashboard-title">🌸 Tableau de bord Étamine</h1>

  <div class="stats-grid">
    <div class="stat-card">
      <h3>💶 Chiffre aujourd’hui</h3>
      <p><%= number_to_currency(@revenue_today, unit: "€") %></p>
    </div>

    <div class="stat-card">
      <h3>📦 Commandes payées</h3>
      <p><%= @orders_count %></p>
    </div>

    <div class="stat-card">
      <h3>📅 Aujourd’hui</h3>
      <p><%= @orders_today.count %> commandes</p>
    </div>
  </div>

  <section class="recent-orders">
    <h2>🧾 Dernières commandes</h2>

    <% if @recent_orders.any? %>
      <ul class="orders-list">
        <% @recent_orders.each do |order| %>
          <li>
            <strong>#<%= order.id %></strong>
            — <%= order.full_name.presence || "Client" %>
            — <%= number_to_currency(order.total_due, unit: "€") %>
            <%= link_to "Voir", admin_order_path(order), class: "btn btn-outline" %>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p>Aucune commande récente.</p>
    <% end %>
  </section>

  <div class="admin-actions">
    <%= link_to "📦 Gérer les commandes", admin_orders_path, class: "btn btn-primary" %>
    <%= link_to "⚙️ Administration technique", rails_admin_path, class: "btn btn-secondary" %>
  </div>
</div>
