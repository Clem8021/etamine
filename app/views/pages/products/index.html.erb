<h1>Boutique</h1>
<ul>
  <% @products.each do |product| %>
    <li>
      <%= image_tag product.image_url, width: 100 %><br>
      <strong><%= product.name %></strong><br>
      <%= number_to_currency(product.price_euros, unit: "€") %><br>
      <%= link_to "Voir", product_path(product) %>
    </li>
  <% end %>
</ul>
