<%= render "shared/navbar" %>

<div class="orders-container">
  <h1 class="orders-title">Mes commandes</h1>
  <% if @orders.any? %>
    <p class="orders-subtitle">
      Voici l’historique de vos commandes réglées 🌸
    </p>
  <% else %>
    <p class="empty-orders">
      Vous n’avez encore aucune commande payée.
    </p>
  <% end %>

  <% if @orders.any? %>
    <div class="orders-grid">
      <% @orders.each do |order| %>
        <div class="order-card">
          <div class="order-card-header">
            <span class="order-number">Commande #<%= order.id %></span>
            <span class="status-badge <%= order.status %>">
              <%= order.status.capitalize %>
            </span>
          </div>

          <div class="order-card-body">
            <p>
              <strong>Date :</strong>
              <%= l(order.created_at, format: :long) %>
            </p>

            <p>
              <strong>Total :</strong>
              <%= number_to_currency(order.total_due, unit: "€") %>
            </p>
          </div>

          <div class="order-card-footer">
            <%= link_to "Voir le détail",
                  order_path(order),
                  class: "btn btn-outline btn-sm" %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="empty-orders">
      <%= link_to "Découvrir la boutique",
            boutique_path,
            class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
