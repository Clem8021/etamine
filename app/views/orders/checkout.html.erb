<div class="floating-back-button">
  <%= link_to root_path, class: "back-link" do %>
    <span>←</span>
    <%= image_tag "logo.png", alt: "Logo Etamine", class: "logo-small" %>
  <% end %>
</div>

<% unless @ready_for_payment %>
  <!-- === Barre de progression (étapes 1/2/3) === -->
  <div class="checkout-progress">
    <div class="step active">
      <span class="circle">1</span>
      <span class="label">Panier</span>
    </div>

    <div class="connector"></div>

    <div class="step">
      <span class="circle">2</span>
      <span class="label">Livraison</span>
    </div>

    <div class="connector"></div>

    <div class="step">
      <span class="circle">3</span>
      <span class="label">Paiement</span>
    </div>
  </div>
<% end %>

<div class="checkout-container">
  <h2><%= @ready_for_payment ? "🧾 Récapitulatif avant paiement" : "🛒 Votre panier" %></h2>

  <% if @order.order_items.any? %>
    <!-- === Récapitulatif des articles === -->
    <div class="order-summary">
      <ul class="order-items-list">
        <% @order.order_items.each do |item| %>
          <li class="order-item">
            <%= image_tag item.product.display_image, alt: item.product.name, class: "product-thumb" %>

            <div class="item-details">
              <p class="item-name"><%= item.product.name %></p>

              <p class="item-meta">
                <% if item.color.present? %>
                  Couleur : <%= item.color %><br>
                <% end %>
                <% if item.size.present? %>
                  Taille : <%= item.size %><br>
                <% end %>
              </p>

              <% unless @ready_for_payment %>
                <div class="quantity-form">
                  <%= form_with model: [@order, item],
                                url: order_order_item_path(@order, item),
                                method: :patch,
                                local: true,
                                class: "inline-form" do |f| %>
                    <label>Quantité :</label>
                    <%= f.number_field :quantity, value: item.quantity, min: 1, class: "quantity-input" %>
                    <%= f.submit "✔", class: "btn-small" %>
                  <% end %>
                </div>

                <div class="item-actions">
                  <%= button_to "❌ Supprimer",
                                order_order_item_path(@order, item),
                                method: :delete,
                                class: "btn-remove",
                                data: { confirm: "Supprimer cet article ?" } %>
                </div>
              <% end %>

              <p><strong><%= number_to_currency(item.total_price, unit: "€") %></strong></p>
            </div>
          </li>
        <% end %>
      </ul>

      <!-- === Totaux === -->
      <div class="order-prices">
        <p>Sous-total : <%= number_to_currency(@order.total_price, unit: "€") %></p>
        <% if @order.delivery_detail&.mode == "delivery" %>
          <p>Frais de livraison : <%= number_to_currency(@order.delivery_fee_cents / 100.0, unit: "€") %></p>
        <% end %>
        <p class="order-total">
          Total TTC : <%= number_to_currency(@order.total_due, unit: "€") %>
        </p>
      </div>
    </div>

    <!-- === Étape 3 : récapitulatif livraison === -->
    <% if @ready_for_payment && @order.delivery_detail.present? %>
      <div class="delivery-summary">
        <h3>🚚 Détails de livraison</h3>
        <% if @order.delivery_detail.mode == "delivery" %>
          <p><strong>Mode :</strong> Livraison à domicile</p>
          <p><strong>Nom :</strong> <%= @order.delivery_detail.recipient_firstname %> <%= @order.delivery_detail.recipient_name %></p>
          <p><strong>Adresse :</strong> <%= @order.delivery_detail.recipient_address %>, <%= @order.delivery_detail.recipient_zip %> <%= @order.delivery_detail.recipient_city %></p>
          <p><strong>Téléphone :</strong> <%= @order.delivery_detail.recipient_phone %></p>
          <p><strong>Date :</strong> <%= @order.delivery_detail.date.strftime("%d/%m/%Y") if @order.delivery_detail.date.present? %></p>
          <% if @order.delivery_detail.time_slot.present? %>
            <p><strong>Créneau horaire :</strong> <%= @order.delivery_detail.time_slot %></p>
          <% end %>
          <% if @order.delivery_detail.notes.present? %>
            <p><strong>Remarques :</strong> <%= @order.delivery_detail.notes %></p>
          <% end %>
        <% else %>
          <p><strong>Mode :</strong> Retrait en boutique</p>
          <p><strong>Date :</strong> <%= @order.delivery_detail.date.strftime("%d/%m/%Y") if @order.delivery_detail.date.present? %></p>
          <p><strong>Créneau :</strong> <%= @order.delivery_detail.time_slot %></p>
        <% end %>
        <%= link_to "✏️ Modifier", edit_order_delivery_detail_path(@order, @order.delivery_detail), class: "btn btn-secondary" %>
      </div>
    <% end %>

    <!-- === Étape 2 ou 3 === -->
        <!-- === Étape 2 ou 3 === -->
    <% unless @ready_for_payment %>
      <div class="checkout-actions">
        <%= link_to "📝 Renseigner mes informations de livraison",
                    new_order_delivery_detail_path(@order),
                    class: "btn btn-secondary" %>

        <%= link_to "🛍 Revenir à la boutique", boutique_path, class: "btn btn-secondary" %>
      </div>
    <% else %>
      <div class="payment-button-wrapper" data-controller="loading">
        <%= button_to "💳 Procéder au paiement sécurisé",
                      confirm_order_path(@order),
                      method: :post,
                      class: "btn btn-primary",
                      data: { turbo: false, action: "loading#activate" } %>

        <!-- 🔙 Bouton retour boutique -->
        <div class="back-to-shop">
          <%= link_to "← Revenir à la boutique", boutique_path, class: "btn btn-secondary" %>
        </div>
      </div>
    <% end %>
  <% else %>
    <!-- === Panier vide === -->
    <div class="empty-cart-notice fade-in">
      <h3>Votre panier est vide 🕊️</h3>
      <p>Ajoutez un bouquet ou une plante pour commencer votre commande.</p>
      <%= link_to "Retour à la boutique", boutique_path, class: "btn-primary" %>
    </div>
  <% end %>
</div>
