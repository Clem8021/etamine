<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Etamine" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Favicon -->
    <%= favicon_link_tag "logo.png" %>
    <%= favicon_link_tag "logo.png", rel: "apple-touch-icon", type: "image/png" %>

    <!-- Styles -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css", media: "all" %>
    <link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@400;700&display=swap" rel="stylesheet">

    <!-- JS -->
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://unpkg.com/@owldesign/stpageflip@2.0.7/dist/css/pageflip.css">
  </head>

    <body data-controller="scroll">

    <% if @shop_closed || @wedding_coming_soon %>
      <div class="shop-modal-overlay">
        <div class="shop-modal">

          <% if @shop_closed %>
            <h2>🌸 Boutique temporairement fermée</h2>
            <p>
              Notre boutique en ligne est momentanément indisponible
              pour des raisons techniques.
            </p>

          <% elsif @wedding_coming_soon %>
            <h2>💍 Mariage – En cours de construction</h2>
            <p>
              Cette section est actuellement en préparation.
            </p>
            <p>
              Nous travaillons sur une offre mariage sur mesure ✨
            </p>
          <% end %>

          <p class="shop-modal-note">
            Merci de votre compréhension 💐
          </p>

          <%= link_to "Retour à l’accueil", root_path, class: "btn btn-primary" %>

        </div>
      </div>
    <% end %>

    <!-- 🔔 Messages flash -->
    <% if flash[:notice] %>
      <div class="flash flash-notice"><%= flash[:notice] %></div>
    <% end %>

    <% if flash[:alert] %>
      <div class="flash flash-alert"><%= flash[:alert] %></div>
    <% end %>

    <!-- 🌸 Contenu principal -->
    <%= yield %>

    <!-- 🌿 Footer -->
    <%= render "shared/footer" unless
      (controller_name == "products" && action_name == "show") ||
      (controller_name == "pages" && action_name == "contact") %>

  </body>
</html>
