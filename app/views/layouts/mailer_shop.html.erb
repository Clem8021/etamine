<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <title>L'Étamine – Nouvelle commande</title>

    <style>
      body {
        margin: 0;
        padding: 0;
        background: #f7f3ef;
        font-family: -apple-system, BlinkMacSystemFont,
                     "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        color: #3e2f2a;
      }

      .mail-wrapper {
        padding: 24px 12px;
      }

      .mail-container {
        max-width: 700px;
        margin: 0 auto;
        background: #ffffff;
        border-radius: 14px;
        overflow: hidden;
        border: 1px solid #e9ddd6;
      }

      .mail-header {
        background: #b96b75;
        color: #ffffff;
        padding: 20px 24px;
      }

      .mail-header h1 {
        margin: 0;
        font-size: 20px;
      }

      .mail-header p {
        margin: 6px 0 0;
        font-size: 13px;
        opacity: 0.95;
      }

      .section {
        padding: 18px 24px;
      }

      .section.alt {
        background: #fffaf7;
        border-top: 1px solid #f0e6e0;
        border-bottom: 1px solid #f0e6e0;
      }

      .h2 {
        margin: 0 0 10px;
        font-size: 16px;
        color: #4a3d36;
      }

      table.kv {
        width: 100%;
        font-size: 14px;
      }

      table.kv td {
        padding: 4px 0;
        vertical-align: top;
      }

      .label {
        width: 160px;
        color: #7a6156;
      }

      .value {
        color: #3e2f2a;
      }

      .items {
        width: 100%;
        border-collapse: collapse;
        font-size: 14px;
      }

      .items th {
        text-align: left;
        padding: 10px 8px;
        border-bottom: 1px solid #e9ddd6;
        color: #7a6156;
        font-weight: 600;
      }

      .items td {
        padding: 10px 8px;
        border-bottom: 1px solid #f3ece7;
      }

      .muted {
        color: #7a6156;
        font-size: 12px;
      }

      .addon-box {
        margin-top: 6px;
        padding: 8px 10px;
        background: #faf6f2;
        border-left: 4px solid #b96b75;
        font-size: 13px;
        white-space: pre-line;
      }

      .tfoot td {
        padding: 8px;
      }

      .total-row td {
        font-weight: 700;
        border-top: 1px solid #e9ddd6;
      }

      .mail-footer {
        padding: 14px 24px;
        background: #faf6f2;
        font-size: 12px;
        color: #7a6156;
        border-top: 1px solid #f0e6e0;
      }
    </style>
  </head>

  <body>
    <div class="mail-wrapper">
      <div class="mail-container">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
